<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eserciziobroadcastreceiver">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EsercizioBroadcastReceiver">
        <activity android:name="com.example.ProgettoAMIF.TransparentActivity" android:theme="@style/MyTranslucentTheme"></activity>
        <activity android:name="com.example.ProgettoAMIF.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.example.broadcast.PhoneUnlockedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <service android:name="com.example.services.TouchDetectService" />
        <service android:name="com.example.model.FasciaOrariaExecutor" />
        <service android:name="com.example.CancelNotificationAndGoHomeScreen" />

    </application>

</manifest>